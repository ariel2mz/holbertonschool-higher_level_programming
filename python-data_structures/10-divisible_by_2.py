#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    a = len(my_list)
    b = my_list.copy()
    for i in range(0, a):
        if (my_list[i] % 2 == 0)
            b[i] = 1
        else
            b[i] = 0
    return b
